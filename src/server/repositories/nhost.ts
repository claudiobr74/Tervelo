import "server-only";

/**
 * Adaptadores Nhost entram aqui na Phase 4+, com codegen contra schema local.
 * Phase 3 define as portas em `src/application/ports.ts` e fakes em memória nos testes.
 */
export {};
