import { AdminInventoryScreen } from "@/components/admin/admin-inventory-screen";

export const metadata = { title: "Inventário da academia — TERVELO Admin" };

export default function AdminInventoryPage() {
  return <AdminInventoryScreen />;
}
