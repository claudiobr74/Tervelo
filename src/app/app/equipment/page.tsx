import { EquipmentCatalogScreen } from "@/components/app/equipment-catalog-screen";

export const metadata = { title: "Equipamentos — TERVELO" };

export default function EquipmentPage() {
  return <EquipmentCatalogScreen />;
}
