import { PlateCalculatorScreen } from "@/components/app/plate-calculator-screen";

export const metadata = { title: "Montagem da barra — TERVELO" };

export default function PlateCalculatorPage() {
  return <PlateCalculatorScreen />;
}
