import { RecoveryCheckinScreen } from "@/components/app/recovery-checkin-screen";

export const metadata = { title: "Recuperação — TERVELO" };

export default function RecoveryPage() {
  return <RecoveryCheckinScreen />;
}
