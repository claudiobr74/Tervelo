import { WeeklyReviewsListScreen } from "@/components/app/weekly-reviews-list-screen";

export const metadata = { title: "Revisões — TERVELO" };

export default function WeeklyReviewsPage() {
  return <WeeklyReviewsListScreen />;
}
