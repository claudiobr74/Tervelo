import { WeeklyReviewScreen } from "@/components/app/weekly-review-screen";

export const metadata = { title: "Revisão Semanal do Coach — TERVELO" };

export default function WeeklyReviewDetailPage() {
  return <WeeklyReviewScreen />;
}
