import { PreWorkoutCheckinScreen } from "@/components/app/pre-workout-checkin-screen";

export const metadata = { title: "Check-in Pré-Treino — TERVELO" };

export default function PreWorkoutCheckinPage() {
  return <PreWorkoutCheckinScreen />;
}
