import { RestTimerScreen } from "@/components/app/rest-timer-screen";

export const metadata = { title: "Descanso — TERVELO" };

export default function WorkoutRestPage() {
  return <RestTimerScreen />;
}
