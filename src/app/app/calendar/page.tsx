import { CalendarScreen } from "@/components/app/calendar-screen";

export const metadata = { title: "Calendário — TERVELO" };

export default function CalendarPage() {
  return <CalendarScreen />;
}
