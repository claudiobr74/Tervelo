import { ProfileAvailabilityScreen } from "@/components/app/profile-availability-screen";

export const metadata = { title: "Disponibilidade — TERVELO" };

export default function ProfileAvailabilityPage() {
  return <ProfileAvailabilityScreen />;
}
