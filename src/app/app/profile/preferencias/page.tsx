import { ProfilePreferencesScreen } from "@/components/app/profile-preferences-screen";

export const metadata = { title: "Preferências de treino — TERVELO" };

export default function ProfilePreferencesPage() {
  return <ProfilePreferencesScreen />;
}
