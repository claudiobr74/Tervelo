# GraphQL

Documents por domínio, um arquivo por operação. Codegen na Phase 3 com schema local.

Não colocar strings GraphQL em componentes.

Pastas: `athlete` (role `user`), `admin` (role `admin` / `super_admin`), e depois `training`, `nutrition`, `recovery`, `body`, `ai`.
