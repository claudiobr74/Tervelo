# Secrets locais da CLI Nhost (`nhost up`). Nunca commitar valores reais.
# Copiar para `.secrets` na raiz do repositório.

HASURA_GRAPHQL_ADMIN_SECRET=change-me
NHOST_WEBHOOK_SECRET=change-me
GRAFANA_ADMIN_PASSWORD=change-me
# JWT RS256 gerados por `nhost up` / `nhost init`
# NHOST_JWT_KID=
# NHOST_JWT_PUBLIC_KEY=
# NHOST_JWT_PRIVATE_KEY=
