table:
  name: ai_rate_limits
  schema: public
select_permissions:
  - role: super_admin
    permission:
      columns:
        - user_id
        - window_started_at
        - request_count
        - updated_at
      filter: {}
      limit: 500
